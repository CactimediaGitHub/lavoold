import extractErrors from 'lavo-mobile/utils/extract-errors';
import { module, test } from 'qunit';

module('Unit | Utility | extract errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = extractErrors();
  assert.ok(result);
});
