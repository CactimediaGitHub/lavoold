import { constant } from 'lavo-mobile/helpers/constant';
import { module, test } from 'qunit';

module('Unit | Helper | constant');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = constant([42]);
  assert.ok(result);
});
