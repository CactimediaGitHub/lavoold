import { price } from 'lavo-mobile/helpers/price';
import { module, test } from 'qunit';

module('Unit | Helper | price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = price([42]);
  assert.ok(result);
});
