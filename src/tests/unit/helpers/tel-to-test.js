import { telTo } from 'lavo-mobile/helpers/tel-to';
import { module, test } from 'qunit';

module('Unit | Helper | tel to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = telTo([42]);
  assert.ok(result);
});
