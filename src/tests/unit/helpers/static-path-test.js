import { staticPath } from 'lavo-mobile/helpers/static-path';
import { module, test } from 'qunit';

module('Unit | Helper | static path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = staticPath([42]);
  assert.ok(result);
});
